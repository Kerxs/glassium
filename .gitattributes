# 换行符。
#
# 仓库里一律存 LF，由 git 在 Windows 上按需转 CRLF —— 这是默认行为。
# 下面这几类**必须**连工作副本都是 LF：
#
#   *.yml   GitHub Actions 的 workflow 在 Linux runner 上解析。YAML 本身容忍
#           CRLF，但 `run: |` 里内联的 shell 不容忍 —— 行尾的 \r 会被 bash
#           当成命令的一部分，报出来的错和真正的原因隔得很远。
#
#   *.wgsl  着色器源在某些驱动上按行做预处理。行尾的 \r 会跟着标识符一起
#           进入 token，编译错误只会告诉你某一行有问题，不会告诉你那是个
#           不可见字符。本项目的 WGSL 目前都内嵌在 .ts 里，但这条规则先立着，
#           免得将来拆成独立文件时踩一遍。
#
* text=auto

*.yml  text eol=lf
*.yaml text eol=lf
*.wgsl text eol=lf
*.glsl text eol=lf
