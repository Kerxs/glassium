{
  // 库的构建：把 src/ 编成 ESM + 类型声明，放进 dist/。不打包、不压缩 —— 使用者的打包器会做。
  // 源码的相对 import 带 .ts 扩展名（Node 原生跑测试要这样写），rewriteRelativeImportExtensions
  // 在产物里把它们改写成 .js。测试不进产物。
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "noEmit": false,
    "declaration": true,
    // 发布的包里不带 src：JS 的 source map 把源码内联进去（调试时照样看得到 TS），声明文件的 map 指向不存在的
    // 源码、没有用，不出
    "declarationMap": false,
    "sourceMap": true,
    "inlineSources": true,
    "outDir": "dist",
    "rootDir": "src",
    "rewriteRelativeImportExtensions": true,
    "types": ["@webgpu/types"],
    "paths": {}
  },
  "include": ["src"],
  "exclude": ["src/**/*.test.ts"]
}
